# Policy artifacts

Generated policies use schema-versioned JSON. Run the portfolio experiment and copy the resulting `policy.json` here before producing a release image. Never commit pickle files or unvalidated model state.
